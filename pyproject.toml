[project]
name = "dm1681-skills"
version = "9.0.0"
description = "Version-controlled coding-agent skills with a cross-platform installer"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "textual>=8.2.8",
]

[tool.uv]
package = false
